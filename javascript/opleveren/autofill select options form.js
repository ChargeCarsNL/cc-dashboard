function autoFillLaadpalen() {

    const keyword = 'repeater_field_3'; // the keyword the elements must contain

    // Use querySelectorAll to select elements with IDs containing the keyword
    const elementsWithKeyword = document.querySelectorAll(`[name*="${keyword}"]`);

    
}